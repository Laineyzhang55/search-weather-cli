# search-weather
It'is a package for search weather

## usage

```bash

npm install weather
```

.js 

```
const weather = require('weather')

weather(city)
.then(data => {
  console.log(data)
}).catch(err => {
  console.log(err)
})
```